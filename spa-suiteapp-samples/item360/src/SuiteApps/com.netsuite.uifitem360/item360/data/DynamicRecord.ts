// Captures field types of the NS dynamic record
enum FieldType {
	TEXT_BOX = 'textbox',
	TEXT_AREA = 'textarea',
	CURRENCY = 'currency',
	CURRENCY2 = 'currency2',
	FLOAT = 'float',
	CHECK_BOX = 'checkbox',
	SELECT = 'select',
	DATE = 'date',
}

const DynamicRecord = {
	FieldType,
};

export default DynamicRecord;
