export default class ContentPanel {
    static HorizontalAlignment = Object.freeze({
        CENTER: 'center',
    });

    static VerticalAlignment = Object.freeze({
        CENTER: 'center',
    })
}