/* eslint-disable new-cap */
define(['./Form', './Sublist'], (Form, Sublist) => {
  const serverWidget = () => {}

  serverWidget.prototype.createForm = () => {}
  serverWidget.prototype.createAssistant = () => {}
  serverWidget.prototype.createList = () => {}

  return new serverWidget()
})
